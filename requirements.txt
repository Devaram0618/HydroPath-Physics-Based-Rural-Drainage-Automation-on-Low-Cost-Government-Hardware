# Rural Drainage Automation Engine - Dependencies # Python 3.12+ # # IMPORTANT: GDAL installation on Windows # ----------------------------------------- # GDAL does not provide pre-built wheels for Python 3.13 on Windows. # Recommended installation methods: # # Option 1: Using conda/mamba (RECOMMENDED) # conda install -c conda-forge gdal # or # mamba install -c conda-forge gdal # # Option 2: Using OSGeo4W # Download from: https://trac.osgeo.org/osgeo4w/ # Install the 'python3-gdal' package # # Option 3: Build from source (requires Visual C++ Build Tools) # py -m pip install --upgrade pip # py -m pip install --upgrade setuptools wheel # py -m pip install GDAL # # Option 4: Use Python 3.11 or 3.12 (wheels available) # py -m pip install GDAL==3.8.0 # # Note: After installing GDAL via conda, ensure the conda environment is activated # before running the pipeline. # Core scientific computing numpy>=2.0.0 scipy>=1.10.0 # Geospatial processing (install via conda-forge recommended) # GDAL>=3.6.0 # Uncomment after installing via conda: conda install -c conda-forge gdal # Visualization matplotlib>=3.8.0 # PDF generation reportlab>=4.0.0 Pillow>=10.0.0 # Configuration PyYAML>=6.0 # Optional: Memory monitoring psutil>=5.9.0 # LAS file processing laspy>=2.4.0 # Note: PDAL and WhiteboxTools are external binaries # - PDAL: https://pdal.io/install.html # - WhiteboxTools: Download from https://www.whiteboxgeo.com/ # Place WhiteboxTools binary at: whitebox/WhiteboxTools_win_amd64/WBT/whitebox_tools.exe # # ── AI Point Cloud Classification (Level 1 Integration) ────────────────── # Optional dependencies for AI-based point cloud classification # These are ONLY required if enable_ai: true in config.yaml # # Scikit-learn for Random Forest classifier scikit-learn>=1.3.0 # ONNX Runtime for deep learning model inference (PointNet, RandLA-Net, etc.) onnxruntime>=1.16.0 # Optional: PyTorch for training custom models (not required for inference) # torch>=2.0.0 # torch>=2.0.0 # Optional: PointNet implementations # pointnet2>=1.0.0 # (if using PointNet++ directly)